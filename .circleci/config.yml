version: 2.1

orbs:
  codecov: codecov/codecov@3
jobs:
  build:
    docker: 
      - image: cimg/node:current
    steps:
      - checkout
      - run:
        name: Install packages
        command: npm install
      - run:
        name: Run tests
        command: node ./node_modules/.bin/istanbul cover tmp_test/tmp_test.js #TODO replace with test script once added. Ex: npm test 
      - codecov/upload
workflow:
  version: 2.1
  build-test: 
    jobs:
      - build

# comment:                 
#   layout: "reach, diff, flags, files"
#   behavior: default
#   require_changes: false  
#   require_base: no        
#   require_head: no  